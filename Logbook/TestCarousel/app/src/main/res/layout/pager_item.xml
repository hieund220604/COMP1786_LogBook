<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Card container enforcing 3:4 aspect ratio; centered horizontally and placed near top so reflection can sit under it -->

    <!-- Reflection immediately beneath the CardView so it aligns with the bottom edge; gap is configurable via layout_marginTop -->

    <androidx.cardview.widget.CardView
        android:id="@+id/card"
        android:layout_width="@dimen/card_width"
        android:layout_height="@dimen/card_height"
        android:layout_marginTop="176dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:foregroundTintMode="src_over"
        app:cardCornerRadius="24dp"
        app:cardElevation="12dp"
        app:cardUseCompatPadding="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.491"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="0dp"
            android:padding="0dp">

            <ImageView
                android:id="@+id/itemImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:adjustViewBounds="true"
                android:background="@drawable/round_rect"
                android:clipToOutline="true"
                android:contentDescription="@string/image_content_description"
                android:scaleType="centerCrop" />

            <!-- Sheen overlay: subtle glossy surface using a gradient with low alpha -->
            <View
                android:id="@+id/sheen"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0.28"
                android:background="@drawable/sheen_overlay"
                android:importantForAccessibility="no" />

            <!-- Soft glow edge (very faint) to emphasize center when scaled up -->
            <View
                android:id="@+id/glow"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0.0"
                android:background="@drawable/soft_edge_glow"
                android:importantForAccessibility="no" />

        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <ImageView
        android:id="@+id/reflection"
        android:layout_width="@dimen/card_width"
        android:layout_height="@dimen/card_height"
        android:layout_marginTop="16dp"
        android:adjustViewBounds="true"
        android:alpha="1.00"
        android:background="@drawable/round_rect"
        android:clipToOutline="true"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop"
        android:visibility="visible"
        app:layout_constraintEnd_toEndOf="@id/card"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="@id/card"
        app:layout_constraintTop_toBottomOf="@id/card" />

</androidx.constraintlayout.widget.ConstraintLayout>
